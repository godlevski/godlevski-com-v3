# deps
node_modules/

# builds
build/
dist/

# wrangler local state (miniflare sqlite, cache)
.wrangler/
.dev.vars

# env
.env.local
.env.*.local
.env.production

# local dev databases at repo root only (regenerate via pnpm db:migrate:local)
/local/

#terraform
infra/generated

# misc
.DS_Store
*.log

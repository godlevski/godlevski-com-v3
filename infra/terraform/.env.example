# cp .env.example .env  (.env is gitignored), then `source .env` before terraform
# token: dash > My Profile > API Tokens > Create Token (see README/main.tf for scopes)
export CLOUDFLARE_API_TOKEN="your-token-here"

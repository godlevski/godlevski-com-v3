# committed dev defaults — override per-machine in .env.development.local
# local dev is same-origin through local-runner (:8080): /api and /files both proxy
PUBLIC_SERVER_BASE=/api
PUBLIC_SLIDE_IMAGE_BASE=/files/slides/
